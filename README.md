# Kuhluhs

An illustration of an async action creator.

## Setup

After cloning this repo:

```sh
npm install
```

To run the server:

```sh
npm run server
```

To run `webpack-dev-server`:

```sh
npm start
```
